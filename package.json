{
  "name": "prometheus",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "dev": "npm run start:dev -w @prometheus/api & npm run dev -w @prometheus/web",
    "prepare": "npm run build -w @prometheus/shared",
    "typecheck": "tsc --noEmit -ws"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
